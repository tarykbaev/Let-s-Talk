<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppButton">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:background">@drawable/app_button</item>
        <item name="android:foreground">?selectableItemBackground</item>
    </style>

    <style name="TransparentButton" parent="AppButton">
        <item name="android:background">@drawable/app_button_transparent</item>
        <item name="android:textColor">@drawable/btn_text_checked</item>
    </style>

    <style name="text_subtitle">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/text_subtitle</item>
        <item name="android:textSize">@dimen/text_18sp</item>
    </style>

    <style name="text_title">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:textSize">@dimen/text_21sp</item>
    </style>

    <style name="SearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="searchIcon">@null</item>
        <item name="queryBackground">@null</item>
        <item name="searchHintIcon">@null</item>
        <item name="queryHint">@string/search</item>
    </style>

    <style name="app_alert_dialog" parent="ThemeOverlay.MaterialComponents.Dialog.Alert">
        <item name="dialogCornerRadius">@dimen/padding_8dp</item>
        <item name="android:textColor">@color/text</item>
        <item name="android:textColorPrimary">@color/primary</item>
    </style>

</resources>